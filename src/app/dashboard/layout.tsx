export const metadata = {
  title: 'Founder Console | Obvious Choice',
  description: 'Protected Founder Console',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {children}
    </>
  )
}
